
const headerStyle = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100vh', // Full height of the viewport (adjust as needed)
};

const Preceder = () => (
  <header style={headerStyle}>
    <img src="/middle-last.svg" alt="Header" />
  </header>
);

export default Preceder;
