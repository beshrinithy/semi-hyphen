const headerStyle = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
     // Full height of the viewport (adjust as needed)
  };
  

  const Newsletter = () => (
      <header style={headerStyle}>
        <img src="/Newsletter.svg" alt="Header"  />
      </header>
    );
    
    export default Newsletter;